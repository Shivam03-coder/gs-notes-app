import React from "react";
import { Button } from "../ui/button";

const RunButton = () => {
  return <Button className="my-2 px-8 text-black bg-green-400 border-black border">Run</Button>;
};

export default RunButton;
